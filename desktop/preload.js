// Intentionally minimal: the game runs entirely as a sandboxed renderer with
// no Node/IPC surface. Nothing to expose yet — kept as a placeholder so main.js
// has a stable preload path if native features (e.g. save-file export) are added later.
